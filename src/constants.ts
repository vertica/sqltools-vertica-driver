import { IDriverAlias } from '@sqltools/types';

/**
 * Aliases for the driver. EG: PostgreSQL, PG, postgres can all resolve to your driver
 */
export const DRIVER_ALIASES: IDriverAlias[] = [{ displayName: "Vertica", value: "Vertica" }];
